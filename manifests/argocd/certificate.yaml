apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-cert
  namespace: argocd
spec:
  secretName: argocd-tls
  issuerRef:
    name: custom-ca-issuer
    kind: ClusterIssuer
  commonName: argocd.yourdomain.com
  dnsNames:
    - argocd.yourdomain.com